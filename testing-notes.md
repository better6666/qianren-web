# 网页版验证记录

## 当前验证结论

默认会话页已渲染为左侧档案索引、中央纸页和右侧边注的桌面工作台布局。网页端已实现浏览器本地语料导入、角色分配、语言画像、离线检索式回复、本地会话、JSON 备份与恢复，以及清空本地数据。外部预览域名需要在 Vite 开发服务中显式允许；已设置开发服务器允许访问。生产静态产物不依赖该开发配置。生成资产在网页预览中以项目存储 URL 引用；桌面可携带包会在打包阶段复制为本地资源并改用相对路径。

已在浏览器载入虚构示例。解析器正确归档 24 条记录，识别出“我”和“小雨”两位说话人，并为其生成可操作的“我 / TA / 忽略”角色分配控件；归档统计与本地记录计数同步更新。

在默认角色分配下，“小雨”被识别为 TA。发送“今天忙不忙？”后，应用创建了本地会话、保存了双方消息，并从 TA 的已归档语料中检索到“还没 在看剧”作为离线回复；会话标题和侧栏会话夹均同步更新。

首次以 `file://` 直接打开当前便携包时出现空白页，且控制台未输出可读错误。已移除仅供托管预览使用的运行时注入后复测，当前受控浏览器仍未在 `file://` 协议下执行模块化前端包。便携包本身采用完全相对路径，不含远程资产；使用说明因此保留“浏览器本地文件策略限制时，请在文件夹中用静态服务器打开”的指引。通过 HTTP 静态服务加载则是生产静态站点的标准使用场景。

## 已知边界

纯静态 HTML 包不含后端，因此不提供原生应用中的本地 Vision OCR，也不保存或转发任何 API Key。离线回复以已归档 TA 语料的相关片段检索为基础，不调用在线模型，也不宣称其为真人回应。
